---
phase: 03-async-infra
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [backend/src/api/routers/documents.py, backend/src/main.py]
autonomous: true
requirements: [INFRA-01, DOC-01]
must_haves:
  truths:
    - "API triggers Celery tasks instead of internal BackgroundTasks"
    - "System handles worker failures gracefully"
    - "Database is updated correctly by workers"
  artifacts:
    - path: "backend/src/api/routers/documents.py"
      provides: "Celery task triggering"
  key_links:
    - from: "backend/src/api/routers/documents.py"
      to: "backend/src/services/tasks.py"
      via: "ingest_document_task.delay()"
---

<objective>
Complete the integration of Celery into the document upload workflow and ensure robustness through error handling and monitoring.

Purpose: Fully enable asynchronous processing and ensure system reliability.
Output: Integrated async pipeline with error handling.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<interfaces>
From backend/src/services/tasks.py:
```python
def ingest_document_task(doc_id: str, workspace_id: str):
    # celery task
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update Document Router to Trigger Celery</name>
  <files>backend/src/api/routers/documents.py</files>
  <action>
    Modify `backend/src/api/routers/documents.py`.
    Replace `background_tasks.add_task(process_document, ...)` with `ingest_document_task.delay(str(doc_id), workspace_id)`.
    
    Remove the `process_document` import and replace it with `ingest_document_task`.
  </action>
  <verify>
    <automated>grep "ingest_document_task.delay" backend/src/api/routers/documents.py</automated>
  </verify>
  <done>API now offloads ingestion to Celery workers</done>
</task>

<task type="auto">
  <name>Task 2: Implement Global Task Monitoring and Cleanup</name>
  <files>backend/src/main.py, backend/src/core/celery_app.py</files>
  <action>
    Ensure Celery workers use WAL mode for SQLite to avoid locking issues:
    Add `PRAGMA journal_mode=WAL;` to database initialization if SQLite is used.
    
    Update `src/main.py` to ensure the Redis Pub/Sub listener starts/stops with the application using FastAPI lifespan.
  </action>
  <verify>
    <automated># Manual verification: Run celery worker and upload a file. 
# Check if task appears in worker logs.</automated>
  </verify>
  <done>System integration complete with robustness improvements</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Async Ingestion Pipeline</what-built>
  <how-to-verify>
    1. Start Redis.
    2. Start FastAPI: `uvicorn src.main:app`.
    3. Start Celery: `celery -A src.core.celery_app worker --loglevel=info`.
    4. Upload a document via the UI or `curl`.
    5. Observe worker logs for task execution.
    6. Observe UI/WebSockets for progress updates (25%, 50%, 75%, 100%).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Worker -> Database | Document status updates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | Celery Queue | mitigate | Limit task concurrency in Celery config if resources are constrained. |
</threat_model>

<success_criteria>
- Documents are processed in the background.
- Workers update the database status correctly.
- UI receives progress updates from the background worker.
</success_criteria>

<output>
After completion, create .planning/phases/03-async-infra/03-03-SUMMARY.md
</output>
